# BillDesk Java POS Billing System

BillDesk is a small full-stack point-of-sale billing application built with plain Java Servlets, JDBC, MySQL, vanilla HTML/CSS/JavaScript, Maven, and a Tomcat WAR deployment. It is intentionally framework-light so the request flow is visible from browser to servlet to DAO to database.

## Current Runtime Target

| Tool | Version | Notes |
|------|---------|-------|
| Java | 21 | Maven compiles with `--release 21`. |
| Maven | 3.6+ | Builds the deployable WAR. |
| Tomcat | 11.x | Provides Jakarta Servlet 6.1 at runtime. |
| MySQL | 8.x | Stores products and checkout transactions. |

Tomcat 11 implements Jakarta Servlet 6.1, so the project uses `jakarta.servlet.*` imports and declares `jakarta.servlet-api` as a `provided` dependency. Do not deploy this WAR to Tomcat 9 or older; those servers use the legacy `javax.servlet.*` namespace.

## Project Layout

```text
billing-system/
|-- pom.xml
|-- README.md
|-- docs/
|   |-- README-frontend.md
|   |-- README-backend.md
|   `-- README-database.md
|-- src/
|   `-- main/
|       |-- java/
|       |   |-- backend/
|       |   |   |-- ProductServlet.java
|       |   |   |-- CheckoutServlet.java
|       |   |   |-- EmailService.java
|       |   |   `-- AppLifecycleListener.java
|       |   `-- database/
|       |       |-- DBConnection.java
|       |       |-- ProductDAO.java
|       |       `-- TransactionDAO.java
|       |-- resources/
|       |   |-- schema.sql
|       |   |-- db.properties
|       |   |-- app.properties
|       |   |-- logo.png
|       |   `-- fonts/
|       |       |-- NotoSans-Regular.ttf
|       |       |-- NotoSans_Condensed-Light.ttf
|       |       `-- NotoSerif-Regular.ttf
|       `-- webapp/
|           |-- index.html
|           |-- style.css
|           |-- script.js
|           |-- logo.png
|           |-- payment.html
|           `-- WEB-INF/
|               `-- web.xml
`-- target/                  generated by Maven, ignored by Git
```

## Architecture

```text
Browser
  index.html + style.css + script.js
  |-- GET  get-product?uid=P001
  `-- POST checkout
          |
          v
Tomcat 11
  ProductServlet
  CheckoutServlet
  AppLifecycleListener
          |
          v
Database layer
  DBConnection -> DBCP2 connection pool
  ProductDAO -> product lookup
  TransactionDAO -> transaction + line-item inserts
          |
          v
MySQL
  products
  transactions
  transaction_items
```

## Setup

1. Create the database.

```bash
mysql -u root -p < src/main/resources/schema.sql
```

2. Configure the database connection.

Edit `src/main/resources/db.properties` for your local MySQL credentials:

```properties
db.url=jdbc:mysql://localhost:3306/billdesk?useSSL=false&serverTimezone=Asia/Kolkata&allowPublicKeyRetrieval=true
db.username=root
db.password=your_password_here
```

This project includes `db.properties` for local development. Do not commit real production credentials to a shared repository.

3. Build the WAR.

```bash
mvn clean package
```

The WAR is created at:

```text
target/billing-system.war
```

4. Deploy to Tomcat 11.

Copy the WAR to Tomcat's `webapps` directory:

```bash
cp target/billing-system.war $CATALINA_HOME/webapps/
```

Then open:

```text
http://localhost:8080/billing-system/
```

## Main Endpoints

| Endpoint | Servlet | Purpose |
|----------|---------|---------|
| `GET /get-product?uid=P001` | `ProductServlet` | Looks up an active product by UID and returns JSON. |
| `POST /checkout` | `CheckoutServlet` | Saves the sale and streams a PDF receipt. |

The frontend uses context-relative URLs (`get-product`, `checkout`). When deployed at `/billing-system`, those resolve correctly under the application context.

## Dependencies

| Dependency | Version | Scope | Used For |
|------------|---------|-------|----------|
| `jakarta.servlet-api` | `6.1.0` | `provided` | Servlet classes supplied by Tomcat 11. |
| `mysql-connector-j` | `8.3.0` | default | JDBC driver for MySQL. |
| `commons-dbcp2` | `2.12.0` | default | Database connection pool. |
| `gson` | `2.10.1` | default | JSON parsing and serialization. |
| `itext-core` | `9.0.0` | default | PDF receipt generation framework. |
| `jakarta.mail-api` | `2.1.3` | default | Jakarta Mail API for dispatching receipts. |
| `jakarta.mail` (Angus) | `2.0.3` | `runtime` | Mail implementation provider. |

## Module Docs

- `docs/README-frontend.md` explains the HTML, CSS, JavaScript cart state, mock mode, and receipt overlay.
- `docs/README-backend.md` explains the servlets, Tomcat 11 / Servlet 6.1 alignment, PDF streaming, and lifecycle listener.
- `docs/README-database.md` explains the schema, connection pool, DAOs, and transaction handling.
